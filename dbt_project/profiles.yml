omnip:
  outputs:
    dev:
      type: athena
      s3_staging_dir: s3://omnip-athena-results-dev-2026/results/
      region_name: us-east-1
      database: awsdatacatalog
      schema: omnip_db_dev
      workgroup: omnip_dev_workgroup # <--- ДОДАЙ ЦЕЙ РЯДОК
      threads: 4
  target: dev
